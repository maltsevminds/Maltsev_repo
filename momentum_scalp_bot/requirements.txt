# Core
ccxt>=4.5.0           # REST + ccxt.pro WebSocket (unified in ccxt>=4)
pandas>=2.2.0
numpy>=1.26.0
# Indicators are implemented natively in momentum_scalp/indicators.py
# (pure pandas/numpy). pandas-ta is intentionally NOT a dependency: it is
# unavailable on Python 3.11 and broken against numpy 2.x.

# Config / validation
pydantic>=2.6.0
PyYAML>=6.0.1
python-dotenv>=1.0.1

# Alerts
python-telegram-bot>=21.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
